name: echo-tool
description: Echo rendered text with an optional prefix.
autoExecuteAllowed: true
tool:
  type: builtin
  command: echo_tool
arguments:
  - name: text
    type: string
    description: Text to echo
    required: true
  - name: prefix
    type: string
    description: Optional prefix
    required: false
    defaultValue: ""
